function __nixhelper_man
    echo
    echo (set_color --bold)"Build commands"
    echo
    echo "       Command | Description"(set_color normal)
    echo "        system | System related actions"
    echo "          home | Home-manager related actions"
end
